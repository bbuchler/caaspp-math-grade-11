# CAASPP Math Handoff - 2026-06-04

## Goal

Build a CAASPP Grade 11 math course that works like the successful Photography / Driver's Ed course pattern:

- Teachers log in to a real gradebook.
- Teachers can add students/teachers, reset passwords, view work samples, print/export, and preview lessons.
- Students log in to a separate student dashboard and complete lessons.
- The course eventually has 4 modules / 20 lessons, but V1 should prove the system with one fully working Lesson 1.
- Lesson 1 should include the overview video, embedded YouTube lesson video, contextual video questions, math practice, exit check, auto-grading, and Study Buddy support.
- The shared Supabase backend must keep course data separated by `course_id`; Driver's Ed, Photography, and CAASPP students must not bleed into each other's gradebooks.

## Current Good State

- GitHub repo is pushed through commit `2308055`.
- Local preview server was restarted and is listening on `http://127.0.0.1:8766`.
- Teacher/admin login routes to `course-package/teacher-dashboard.html`.
- `admin-dashboard.html` redirects to the teacher gradebook.
- Lesson preview has a role-aware header:
  - teacher/admin: Gradebook, Teacher Guide, Course Home, Sign Out
  - student: Student Dashboard, Course Home, Sign Out
- Login page no longer exposes course/gradebook preview links.
- Lesson 1 JSON has the YouTube no-cookie embed for `bAerID24QJ0`.
- Lesson 1 overview MP4 and poster are served locally from `course-package/media/videos`.
- Gradebook no longer loads all student profiles. It now reads CAASPP students through `course_lesson_score_summary` scoped to `course_id = caaspp-math-11`.
- Course Studio memory records the rule: never load shared-backend gradebooks from global `profiles`; always scope by `course_enrollments.course_id`.
- 2026-06-05 check: the local `.env` has `GEMINI_API_KEY` but does not have `SUPABASE_SERVICE_ROLE_KEY`. The local Add Account API returns `Server configuration error: missing service role key.`

## What Is Still Not Done

- Add Account and Reset Password are wired to local/server API routes, but they need `SUPABASE_SERVICE_ROLE_KEY` in `.env` and deployment env before they can create/reset real Supabase Auth users.
- There is not yet a confirmed CAASPP test student account.
- If the local `.env` does not include `SUPABASE_SERVICE_ROLE_KEY`, Add Account / Reset Password will return a server configuration error.
- The teacher dashboard still needs a final Photography-style polish pass after the test-student flow works.
- The gradebook should show zero CAASPP students until a student is enrolled in `course_enrollments` for `caaspp-math-11`.
- Lesson 1 work sample flow needs to be verified after a real test student completes responses.
- Visual QA was not fully completed because the bundled Playwright dependency was broken in this environment.

## Tomorrow's Pickup Plan

1. Refresh `http://127.0.0.1:8766/course-package/teacher-dashboard.html`.
2. Confirm Driver's Ed students no longer appear.
3. Add the Driver's Ed/shared Supabase `SUPABASE_SERVICE_ROLE_KEY` to `.env` and later deployment env.
4. Restart the local server.
5. Use Add Account to create one CAASPP test student.
6. Log in as that test student and complete Lesson 1.
7. Confirm responses save to Supabase and only appear in CAASPP gradebook.
8. Open the test student's work sample from the gradebook.
9. Test Reset Password on the CAASPP test student.
10. Polish teacher dashboard layout to match Photography more closely.

## Temporary Fallback If Service Key Is Not Available

Create one test student in Supabase Authentication manually, then run `course-package/supabase/enroll-existing-user-as-caaspp-student.template.sql` with that student's email. This lets Lesson 1 be tested without dashboard account creation, but it is not the long-term workflow.

## Important Lesson From Today

The shared Supabase project is fine only if every course feature is course-scoped. The bug today was not Supabase itself; it was the dashboard code pulling from global `profiles`, which made Driver's Ed students appear in CAASPP. Do not repeat that pattern.
